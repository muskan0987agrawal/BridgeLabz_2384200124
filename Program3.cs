/*using System;
class Program3 {

     //create a function to perform a DoubleOperation
	 void calculateDoubleOperation(){
	 
	 Console.Write("Enter the value of a: ");
	double a = double.Parse(Console.ReadLine()); //Converting a String to an double
	 
	 Console.Write("Enter the value of b: ");
	double b = double.Parse(Console.ReadLine()); //Converting a String to an double
	 
	 Console.Write("Enter the value of c: ");
	double c = double.Parse(Console.ReadLine()); //Converting a String to an double
	 
	 //Perform some Operation
	double operation1 = a + b * c;
	 double operation2 = a * b + c;
	double operation3 = c + a / b;
	double operation4 = a % b + c;
	 
	 //Printing all the operations 
     Console.WriteLine($"the results of double Operations are {operation1}, {operation2}, {operation3}, {operation4}");
     Console.ReadLine(); // to holds the console screen
	 }

      static void Main(string[] args){
        Program3 P = new Program3(); //creating an object of class
		P.calculateDoubleOperation();  // access the function calculateDoubleOperation
		}
	}
        	  
	*/