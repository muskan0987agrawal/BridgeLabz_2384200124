/* using System;

class FactorOfNumber
{
    static void FindFactors()
    {
        // Input the number from the user
        Console.Write("Enter a number to find its factors: ");
        int number = int.Parse(Console.ReadLine());

        Console.WriteLine($"Factors of {number} are:");

        // Loop through 1 to the number to check for factors
        for (int i = 1; i <= number; i++)
        {
            if (number % i == 0) Console.Write(i + " ");
        }
    }

    // Main method
    static void Main()
    {
        FindFactors(); // Call the method to find factors
    }
}
*/