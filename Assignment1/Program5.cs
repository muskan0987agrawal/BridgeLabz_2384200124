/*using System;

 class Program5{
   //create a function to check whether a person can vote or not
   void canGiveVote(){
   
     Console.Write("Enter the age: ");
	 int age = int.Parse(Console.ReadLine());
	 
	 if(age >= 18){
	 Console.WriteLine($"The Person's age is {age} and can vote");
	 }
	 
	 else{
	 Console.WriteLine($"The Person's age is {age} and cannot vote");
	 }
	 
	}
	static void Main(){
	Program5 p = new Program5(); //create a object of class
     p.canGiveVote(); 
	 
	}
	}
	 */