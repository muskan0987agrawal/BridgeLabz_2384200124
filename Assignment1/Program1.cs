/*using System;
class Program1{ 

     //create a function to Check number is divisible by 5 or not
	 
     void checkNumberIsDivisibleBy5(){
   
     //get the input
	  Console.Write("Enter the number: ");
	  int number = int.Parse(Console.ReadLine());
	   
     // Check if the number is divisible by 5
        bool isDivisibleBy5 = number % 5 == 0;

      // Output the result
        Console.WriteLine($"Is the number {number} divisible by 5? {isDivisibleBy5}");
    }
	
	static void Main(){
	Program1 p = new Program1(); //create a object of class
     p.checkNumberIsDivisibleBy5(); //access the function
}
}	
*/ 
	